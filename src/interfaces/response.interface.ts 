// src/interfaces/response.interface.ts

export interface IBaseResponse {
  status: string;
  message: string;
}

export interface IResponse<T = any> extends IBaseResponse {
  data?: T;
  error?: any;
  meta?: any;
}

export type ResponseType = IBaseResponse & IResponse<any>;

export type UserAuthResponseType = IResponse<IUserAuth>;
